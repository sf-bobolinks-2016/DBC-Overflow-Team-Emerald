<div>
  <ul>
    <% @question.comments.each do |comment| %>
      <li><%= "#{comment.comment} (#{comment.user.email})" %></li>
    <% end %>
  </ul>
</div>


<!-- Comment.where(answer_id: nil) -->
<!-- Comment.where(question: nil) -->
